func fib(n: Int) -> Int:
{
	if (n == 1 || n == 2) return 1;
	return fib(n - 2) + fib(n - 1);
}

func main(argc: Int, argv: Array<String>):
{
	var n: Int;
	while (True)
	{
		print("Enter integer number: ");
		scan("%d", n);
		print("fib(%d) = %d\n", n, fib(n));
	}
}
